package ${package};

import org.apache.ibatis.annotations.Mapper;
import ${model};

/**
 * 테이블 명 : ${tableName}
 * 설명       : ${desc}
 */
@Mapper
public interface ${fileName}Dao {
	
     /**
     * 
     * ${fileName}Dao는 각 프로젝트에서 공통으로 사용되는 영역을 구현 합니다.
     * 되도록 실제 중요한 업무이면서 각 프로젝트에서 공유 되어야할 경우에만 작성하도록 합니다.
     * 
     */

}
