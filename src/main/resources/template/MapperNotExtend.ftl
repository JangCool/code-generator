<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${mapperid}">

    <!--
		※ 테이블 명  : ${name}
		※설명         : ${desc}
		
		※ 샘플 입니다. 필요한 부분 발췌하여 사용하시기 바랍니다.
		
	    <resultMap id="Base${fileName}ResultMap" type="${model}" >
	    	${resultMap}
	    </resultMap>
	    
	    <sql id="select-columns">
	    	${columns}
	    </sql>
	    
	    <sql id="pk-columns">
	    	${pkcolumns}
	    </sql>
	    
        <select id="findBy조건명" parameterType="${model}" resultType="${model}">
		${findBy} 
	    </select>
	    
	    <insert id="insert조건명" parameterType="${model}">	
		${insert} 
	    </insert>

		<update id="update조건명" parameterType="${model}">
		${update}   
		</update>
		
		<delete id="delete조건명" parameterType="${model}">
		${delete}  
		</delete>
    -->

</mapper>